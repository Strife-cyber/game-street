---
// Configuration de la page
const title = 'Game Street - Semaine de la Diversité';
const description =
	"Événement gaming d'une semaine pendant la Semaine de la Diversité ! VR, PS5, PC Gaming, Tournois et plus encore !";
// Dates: 31 Octobre - 7 Novembre 2025
---

<!doctype html>
<html lang="fr">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<meta name="generator" content={Astro.generator} />
		<meta name="description" content={description} />
		<title>{title}</title>
		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<link
			href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&family=Exo+2:wght@300;400;600;700&display=swap"
			rel="stylesheet"
		/>
	</head>
	<body>
		<!-- Navigation -->
		<nav class="nav">
			<div class="nav-container">
				<a href="/" class="logo">
					<span class="logo-icon">
						<!--🎮-->
						<img src="/images/street.jpg" alt="Logo Game Street" width="48" height="48" />
					</span>
					<span class="logo-text">GAME STREET</span>
				</a>
				<ul class="nav-links">
					<li><a href="/">Accueil</a></li>
					<li><a href="/games">Jeux</a></li>
					<li><a href="/#events">Activités</a></li>
					<li><a href="/#features">Équipement</a></li>
					<li><a href="/#contact">Contact</a></li>
				</ul>
				<button class="mobile-menu-btn" aria-label="Menu">
					<span></span>
					<span></span>
					<span></span>
				</button>
			</div>
		</nav>

		<!-- Hero Section avec animation 3D -->
		<section id="home" class="hero">
			<div class="hero-bg">
				<div class="animated-grid"></div>
				<div class="particles"></div>
			</div>

			<div class="hero-content">
				<div class="hero-title-container">
					<h1 class="hero-title" data-text="GAME STREET">
						<span class="title-3d">GAME STREET</span>
					</h1>
					<p class="hero-subtitle">SEMAINE DE LA DIVERSITÉ</p>
				</div>

				<p class="hero-description">
					Venez découvrir Game Street pendant la Semaine de la Diversité ! Une semaine complète
					dédiée au gaming : VR, PS5, PS4, PC Gaming haute performance avec écrans 2K, tournois et
					bien plus encore. Ouvert à tous !
				</p>

				<div class="hero-buttons">
					<button class="btn btn-primary">
						<span>Rejoindre Maintenant</span>
						<div class="btn-glow"></div>
					</button>
					<button class="btn btn-secondary">
						<span>Découvrir Plus</span>
					</button>
				</div>

				<div class="hero-stats">
					<div class="stat-card">
						<div class="stat-number">1 SEMAINE</div>
						<div class="stat-label">Événement</div>
					</div>
					<div class="stat-card">
						<div class="stat-number">VR + CONSOLES</div>
						<div class="stat-label">Multi-plateforme</div>
					</div>
					<div class="stat-card">
						<div class="stat-number">TOURNOIS</div>
						<div class="stat-label">Compétitions</div>
					</div>
				</div>
			</div>

			<!-- TODO: Ajouter une image de hero gaming ici -->
			<!-- Recommandé: Image de gamers en action, résolution 1920x1080 -->
			<!-- <div class="hero-image-placeholder">
				<div class="placeholder-content">
					<span class="placeholder-icon">🖼️</span>
					<p>Image Hero Gaming</p>
					<small>1920x1080px recommandé</small>
				</div>
			</div> -->
		</section>

		<!-- Section Événements -->
		<section id="events" class="events-section">
			<div class="container">
				<h2 class="section-title">
					<span class="title-glow">Ce Que Nous Offrons</span>
				</h2>

				<div class="events-grid">
					<!-- Event Card 1 -->
					<div class="event-card card-3d">
						<div class="card-inner">
							<div class="event-image-placeholder">
								<img
									src="/images/event-tournament.webp"
									alt="Tournois Gaming"
									class="event-image"
								/>
							</div>

							<div class="event-content">
								<div class="event-date">31 OCT - 7 NOV</div>
								<h3>Tournois Gaming</h3>
								<p>
									Participez à nos tournois sur différents jeux ! Affrontez vos camarades et montrez
									vos compétences. Plusieurs tournois organisés pendant la semaine.
								</p>
								<div class="event-tags">
									<span class="tag">Compétition</span>
									<span class="tag">Tous niveaux</span>
								</div>
								<button class="btn-event">Participer →</button>
							</div>
						</div>
					</div>

					<!-- Event Card 2 -->
					<div class="event-card card-3d">
						<div class="card-inner">
							<div class="event-image-placeholder">
								<img src="/images/event-lan.webp" alt="PC Gaming & Consoles" class="event-image" />
							</div>

							<div class="event-content">
								<div class="event-date">31 OCT - 7 NOV</div>
								<h3>PC Gaming & Consoles</h3>
								<p>
									Jouez sur nos PC haute performance avec écrans 2K, PS5, PS4 ou laptops ! Tous vos
									jeux préférés disponibles.
								</p>
								<div class="event-tags">
									<span class="tag">PS5/PS4</span>
									<span class="tag">PC 2K</span>
									<span class="tag">Laptops</span>
								</div>
								<button class="btn-event">Venir Jouer →</button>
							</div>
						</div>
					</div>

					<!-- Event Card 3 -->
					<div class="event-card card-3d">
						<div class="card-inner">
							<div class="event-image-placeholder">
								<span>📸 Image VR Gaming</span>
							</div>

							<div class="event-content">
								<div class="event-date">31 OCT - 7 NOV</div>
								<h3>Expérience VR</h3>
								<p>
									Découvrez la réalité virtuelle ! Plongez dans des mondes immersifs et vivez des
									expériences gaming inédites avec nos casques VR.
								</p>
								<div class="event-tags">
									<span class="tag">VR</span>
									<span class="tag">Immersif</span>
								</div>
								<button class="btn-event">Essayer VR →</button>
							</div>
						</div>
					</div>
				</div>
			</div>
		</section>

		<!-- Section Fonctionnalités -->
		<section id="features" class="features-section">
			<div class="container">
				<h2 class="section-title">
					<span class="title-glow">Notre Équipement</span>
				</h2>

				<div class="features-grid">
					<div class="feature-card floating">
						<div class="feature-icon">🥽</div>
						<h3>Réalité Virtuelle</h3>
						<p>
							Découvrez l'immersion totale avec nos casques VR dernier cri pour des expériences
							gaming uniques
						</p>
					</div>

					<div class="feature-card floating">
						<div class="feature-icon">🎮</div>
						<h3>PS5 & PS4</h3>
						<p>
							Jouez sur les dernières consoles PlayStation avec une bibliothèque complète de jeux
							populaires
						</p>
					</div>

					<div class="feature-card floating">
						<div class="feature-icon">🖥️</div>
						<h3>PC Gaming 2K</h3>
						<p>
							PC haute performance équipés d'écrans 2K pour une qualité graphique exceptionnelle
						</p>
					</div>

					<div class="feature-card floating">
						<div class="feature-icon">💻</div>
						<h3>Laptops Gaming</h3>
						<p>Laptops performants disponibles pour jouer à vos jeux favoris en toute mobilité</p>
					</div>

					<div class="feature-card floating">
						<div class="feature-icon">🏆</div>
						<h3>Tournois</h3>
						<p>
							Participez à nos tournois organisés toute la semaine sur différents jeux et
							plateformes
						</p>
					</div>

					<div class="feature-card floating">
						<div class="feature-icon">🌈</div>
						<h3>Ouvert à Tous</h3>
						<p>
							Événement inclusif pendant la Semaine de la Diversité - tous les niveaux sont les
							bienvenus !
						</p>
					</div>
				</div>
			</div>
		</section>

		<!-- Section Jeux Disponibles -->
		<section class="games-promo-section">
			<div class="container">
				<div class="games-promo-card">
					<h2 class="games-promo-title">
						<span class="games-icon">🎮</span>
						Découvrez Tous Nos Jeux !
					</h2>
					<p class="games-promo-text">
						FIFA, Naruto, Call of Duty, Fortnite, Street Fighter et bien plus encore !
						<br />
						<strong>Plus de 24 jeux</strong> disponibles sur PS5, PS4, PC et VR.
					</p>
					<a href="/games" class="btn-games-promo">
						<span>Voir Tous les Jeux →</span>
						<div class="btn-glow"></div>
					</a>
				</div>
			</div>
		</section>

		<!-- Section Communauté -->
		<section id="community" class="community-section">
			<div class="container">
				<h2 class="section-title">
					<span class="title-glow">Notre Communauté</span>
				</h2>

				<div class="community-grid">
					<div class="community-card pulse-3d">
						<!-- TODO: Ajouter image de la communauté 1 -->
						<!-- Recommandé: Photo de groupe de gamers, 600x400px -->
						<div class="community-image-placeholder">
							<span>📸 Photo Communauté 1</span>
						</div>
						<div class="community-overlay">
							<h3>Rencontres Hebdomadaires</h3>
							<p>Retrouvez-nous chaque semaine</p>
						</div>
					</div>

					<div class="community-card pulse-3d">
						<!-- TODO: Ajouter image de la communauté 2 -->
						<!-- Recommandé: Setup gaming, 600x400px -->
						<div class="community-image-placeholder">
							<span>📸 Photo Communauté 2</span>
						</div>
						<div class="community-overlay">
							<h3>Setups Incroyables</h3>
							<p>Partagez votre battle station</p>
						</div>
					</div>

					<div class="community-card pulse-3d">
						<!-- TODO: Ajouter image de la communauté 3 -->
						<!-- Recommandé: Événement gaming, 600x400px -->
						<div class="community-image-placeholder">
							<span>📸 Photo Communauté 3</span>
						</div>
						<div class="community-overlay">
							<h3>Événements Épiques</h3>
							<p>Des moments inoubliables</p>
						</div>
					</div>

					<div class="community-card pulse-3d">
						<!-- TODO: Ajouter image de la communauté 4 -->
						<!-- Recommandé: Streamers/joueurs, 600x400px -->
						<div class="community-image-placeholder">
							<span>📸 Photo Communauté 4</span>
						</div>
						<div class="community-overlay">
							<h3>Streamers Partenaires</h3>
							<p>Suivez nos créateurs</p>
						</div>
					</div>
				</div>
			</div>
		</section>

		<!-- Gallery Showcase avec effet 3D -->
		<section class="gallery-section">
			<div class="container">
				<h2 class="section-title">
					<span class="title-glow">Galerie Épique</span>
				</h2>
				<div class="gallery-grid">
					<!-- TODO: Ajouter 6 images de galerie -->
					<!-- Recommandé: Images de moments gaming épiques, 800x600px -->
					<div class="gallery-item">
						<div class="gallery-placeholder">
							<span>📸 Image 1</span>
						</div>
					</div>
					<div class="gallery-item">
						<div class="gallery-placeholder">
							<span>📸 Image 2</span>
						</div>
					</div>
					<div class="gallery-item">
						<div class="gallery-placeholder">
							<span>📸 Image 3</span>
						</div>
					</div>
					<div class="gallery-item">
						<div class="gallery-placeholder">
							<span>📸 Image 4</span>
						</div>
					</div>
					<div class="gallery-item">
						<div class="gallery-placeholder">
							<span>📸 Image 5</span>
						</div>
					</div>
					<div class="gallery-item">
						<div class="gallery-placeholder">
							<span>📸 Image 6</span>
						</div>
					</div>
				</div>
			</div>
		</section>

		<!-- Info Section -->
		<section class="newsletter-section">
			<div class="container">
				<div class="newsletter-card">
					<h2>Venez Nous Voir !</h2>
					<p>Événement gratuit et ouvert à tous pendant la Semaine de la Diversité</p>
					<div class="event-info">
						<p><strong>📅 Dates :</strong> 31 Octobre - 7 Novembre 2025</p>
						<p><strong>📍 Lieu :</strong> [Votre emplacement à l'école]</p>
						<p><strong>⏰ Horaires :</strong> [Vos horaires]</p>
					</div>
				</div>
			</div>
		</section>

		<!-- Footer -->
		<footer id="contact" class="footer">
			<div class="container">
				<div class="footer-grid">
					<div class="footer-col">
						<h3>Game Street</h3>
						<p>Événement gaming pendant la Semaine de la Diversité à l'école</p>
						<div class="social-links">
							<a href="#" class="social-icon" title="Instagram">📷</a>
							<a href="#" class="social-icon" title="Discord">💬</a>
							<a href="#" class="social-icon" title="TikTok">📱</a>
							<a href="#" class="social-icon" title="Email">✉️</a>
						</div>
					</div>

					<div class="footer-col">
						<h4>Navigation</h4>
						<ul>
							<li><a href="#home">Accueil</a></li>
							<li><a href="#events">Ce qu'on offre</a></li>
							<li><a href="#features">Équipement</a></li>
							<li><a href="#community">Galerie</a></li>
						</ul>
					</div>

					<div class="footer-col">
						<h4>Infos Pratiques</h4>
						<ul>
							<li>📅 31 Oct - 7 Nov 2025</li>
							<li>🎮 VR, PS5, PS4, PC</li>
							<li>🏆 Tournois quotidiens</li>
							<li>🎟️ Entrée gratuite</li>
						</ul>
					</div>

					<div class="footer-col">
						<h4>Contact</h4>
						<ul>
							<li>📧 [votre-email]</li>
							<li>📱 [votre-numéro]</li>
							<li>📍 [Votre école]</li>
							<li>⏰ [Vos horaires]</li>
						</ul>
					</div>
				</div>

				<div class="footer-bottom">
					<p>&copy; 2025 Game Street - Semaine de la Diversité</p>
				</div>
			</div>
		</footer>

		<!-- Scroll to top button -->
		<button class="scroll-to-top" id="scrollToTop" aria-label="Retour en haut"> ↑ </button>

		<style>
			/* ============================================
			   RESET & VARIABLES
			   ============================================ */
			* {
				margin: 0;
				padding: 0;
				box-sizing: border-box;
			}

			:root {
				/* Couleurs principales avec thème gaming néon */
				--primary: #00f0ff;
				--secondary: #ff00ff;
				--accent: #ffff00;
				--dark: #0a0a0f;
				--dark-2: #1a1a2e;
				--dark-3: #16213e;
				--light: #ffffff;
				--gray: #8892b0;

				/* Gradients inspirés gaming */
				--gradient-1: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
				--gradient-2: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
				--gradient-3: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
				--gradient-neon: linear-gradient(135deg, var(--primary), var(--secondary));
				--gradient-gaming: linear-gradient(135deg, #12c2e9, #c471ed, #f64f59);

				/* Typographie moderne et futuriste */
				--font-primary: 'Orbitron', sans-serif;
				--font-secondary: 'Exo 2', sans-serif;

				/* Espacements */
				--spacing-xs: 0.5rem;
				--spacing-sm: 1rem;
				--spacing-md: 2rem;
				--spacing-lg: 4rem;
				--spacing-xl: 6rem;

				/* Effets de profondeur et ombres */
				--shadow-sm: 0 2px 10px rgba(0, 240, 255, 0.1);
				--shadow-md: 0 10px 30px rgba(0, 240, 255, 0.2);
				--shadow-lg: 0 20px 60px rgba(0, 240, 255, 0.3);
				--shadow-neon: 0 0 20px var(--primary), 0 0 40px var(--primary);
			}

			html {
				scroll-behavior: smooth;
			}

			body {
				font-family: var(--font-secondary);
				background-color: var(--dark);
				color: var(--light);
				line-height: 1.6;
				overflow-x: hidden;
			}

			.container {
				max-width: 1400px;
				margin: 0 auto;
				padding: 0 var(--spacing-md);
			}

			/* ============================================
			   NAVIGATION
			   ============================================ */
			.nav {
				position: fixed;
				top: 0;
				left: 0;
				right: 0;
				z-index: 1000;
				background: rgba(10, 10, 15, 0.95);
				backdrop-filter: blur(10px);
				border-bottom: 1px solid rgba(0, 240, 255, 0.1);
				animation: slideDown 0.5s ease-out;
			}

			@keyframes slideDown {
				from {
					transform: translateY(-100%);
				}
				to {
					transform: translateY(0);
				}
			}

			.nav-container {
				max-width: 1400px;
				margin: 0 auto;
				padding: 1rem 2rem;
				display: flex;
				justify-content: space-between;
				align-items: center;
			}

		.logo {
			display: flex;
			align-items: center;
			gap: 0.5rem;
			font-family: var(--font-primary);
			font-size: 1.5rem;
			font-weight: 900;
			color: var(--primary);
			text-shadow: 0 0 20px var(--primary);
			cursor: pointer;
			transition: transform 0.3s ease;
			text-decoration: none;
		}

			.logo:hover {
				transform: scale(1.05);
			}

			.logo-icon {
				font-size: 2rem;
				animation: float 3s ease-in-out infinite;
			}

			@keyframes float {
				0%,
				100% {
					transform: translateY(0px);
				}
				50% {
					transform: translateY(-10px);
				}
			}

			.nav-links {
				display: flex;
				gap: 2rem;
				list-style: none;
			}

			.nav-links a {
				color: var(--gray);
				text-decoration: none;
				font-weight: 600;
				position: relative;
				transition: color 0.3s ease;
			}

			.nav-links a::after {
				content: '';
				position: absolute;
				bottom: -5px;
				left: 0;
				width: 0;
				height: 2px;
				background: var(--gradient-neon);
				transition: width 0.3s ease;
			}

			.nav-links a:hover {
				color: var(--primary);
			}

			.nav-links a:hover::after {
				width: 100%;
			}

			.mobile-menu-btn {
				display: none;
				flex-direction: column;
				gap: 5px;
				background: none;
				border: none;
				cursor: pointer;
				padding: 5px;
			}

			.mobile-menu-btn span {
				width: 25px;
				height: 3px;
				background: var(--primary);
				border-radius: 2px;
				transition: all 0.3s ease;
			}

			/* ============================================
			   HERO SECTION
			   ============================================ */
			.hero {
				position: relative;
				min-height: 100vh;
				display: flex;
				align-items: center;
				justify-content: center;
				overflow: hidden;
				padding-top: 80px;
			}

			.hero-bg {
				position: absolute;
				top: 0;
				left: 0;
				right: 0;
				bottom: 0;
				z-index: 0;
			}

			/* Grille animée en arrière-plan avec perspective 3D */
			.animated-grid {
				position: absolute;
				width: 100%;
				height: 100%;
				background-image:
					linear-gradient(rgba(0, 240, 255, 0.1) 1px, transparent 1px),
					linear-gradient(90deg, rgba(0, 240, 255, 0.1) 1px, transparent 1px);
				background-size: 50px 50px;
				animation: gridMove 20s linear infinite;
				transform: perspective(500px) rotateX(60deg);
				transform-origin: center center;
			}

			@keyframes gridMove {
				0% {
					background-position: 0 0;
				}
				100% {
					background-position: 50px 50px;
				}
			}

			/* Particules animées avec effet de profondeur */
			.particles {
				position: absolute;
				width: 100%;
				height: 100%;
			}

			.particles::before,
			.particles::after {
				content: '';
				position: absolute;
				width: 300px;
				height: 300px;
				border-radius: 50%;
				filter: blur(100px);
				opacity: 0.3;
				animation: particleFloat 10s ease-in-out infinite;
			}

			.particles::before {
				background: var(--primary);
				top: 20%;
				left: 10%;
				animation-delay: 0s;
			}

			.particles::after {
				background: var(--secondary);
				bottom: 20%;
				right: 10%;
				animation-delay: 5s;
			}

			@keyframes particleFloat {
				0%,
				100% {
					transform: translate(0, 0) scale(1);
				}
				33% {
					transform: translate(50px, -50px) scale(1.1);
				}
				66% {
					transform: translate(-50px, 50px) scale(0.9);
				}
			}

			.hero-content {
				position: relative;
				z-index: 10;
				text-align: center;
				max-width: 1200px;
				padding: var(--spacing-md);
			}

			.hero-title-container {
				perspective: 1000px;
				margin-bottom: var(--spacing-md);
			}

			.hero-title {
				font-family: var(--font-primary);
				font-size: clamp(3rem, 10vw, 8rem);
				font-weight: 900;
				margin-bottom: var(--spacing-sm);
				position: relative;
			}

			/* Titre 3D avec animation de rotation subtile */
			.title-3d {
				display: inline-block;
				background: var(--gradient-neon);
				-webkit-background-clip: text;
				background-clip: text;
				-webkit-text-fill-color: transparent;
				animation: title3DAnimation 5s ease-in-out infinite;
				filter: drop-shadow(0 10px 20px rgba(0, 240, 255, 0.5));
			}

			@keyframes title3DAnimation {
				0%,
				100% {
					transform: rotateY(0deg) rotateX(0deg);
				}
				25% {
					transform: rotateY(5deg) rotateX(2deg);
				}
				50% {
					transform: rotateY(0deg) rotateX(-2deg);
				}
				75% {
					transform: rotateY(-5deg) rotateX(2deg);
				}
			}

			.hero-subtitle {
				font-size: 1.5rem;
				font-weight: 700;
				letter-spacing: 0.3rem;
				color: var(--primary);
				text-shadow: var(--shadow-neon);
				animation: pulse 2s ease-in-out infinite;
			}

			@keyframes pulse {
				0%,
				100% {
					opacity: 1;
				}
				50% {
					opacity: 0.7;
				}
			}

			.hero-description {
				font-size: 1.2rem;
				color: var(--gray);
				max-width: 800px;
				margin: var(--spacing-md) auto;
				line-height: 1.8;
			}

			.hero-buttons {
				display: flex;
				gap: var(--spacing-sm);
				justify-content: center;
				flex-wrap: wrap;
				margin: var(--spacing-md) 0;
			}

			/* Boutons avec effets 3D et animations */
			.btn {
				position: relative;
				padding: 1rem 2.5rem;
				font-size: 1.1rem;
				font-weight: 700;
				font-family: var(--font-primary);
				border: none;
				border-radius: 50px;
				cursor: pointer;
				transition: all 0.3s ease;
				overflow: hidden;
				transform-style: preserve-3d;
			}

			.btn span {
				position: relative;
				z-index: 2;
			}

			.btn-primary {
				background: var(--gradient-neon);
				color: var(--dark);
				box-shadow: var(--shadow-lg);
			}

			.btn-primary:hover {
				transform: translateY(-5px) scale(1.05);
				box-shadow: 0 20px 60px rgba(0, 240, 255, 0.5);
			}

			.btn-glow {
				position: absolute;
				top: 0;
				left: -100%;
				width: 100%;
				height: 100%;
				background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.5), transparent);
				transition: left 0.5s ease;
			}

			.btn-primary:hover .btn-glow {
				left: 100%;
			}

			.btn-secondary {
				background: transparent;
				color: var(--primary);
				border: 2px solid var(--primary);
			}

			.btn-secondary:hover {
				background: var(--primary);
				color: var(--dark);
				transform: translateY(-5px) scale(1.05);
				box-shadow: var(--shadow-md);
			}

			/* Stats avec effet 3D et animation de flottement */
			.hero-stats {
				display: flex;
				gap: var(--spacing-md);
				justify-content: center;
				flex-wrap: wrap;
				margin-top: var(--spacing-lg);
			}

			.stat-card {
				background: rgba(26, 26, 46, 0.8);
				padding: 2rem 3rem;
				border-radius: 20px;
				border: 1px solid rgba(0, 240, 255, 0.2);
				backdrop-filter: blur(10px);
				transform: translateZ(50px);
				transition: all 0.3s ease;
				animation: statFloat 3s ease-in-out infinite;
			}

			.stat-card:nth-child(1) {
				animation-delay: 0s;
			}
			.stat-card:nth-child(2) {
				animation-delay: 0.5s;
			}
			.stat-card:nth-child(3) {
				animation-delay: 1s;
			}

			@keyframes statFloat {
				0%,
				100% {
					transform: translateY(0) translateZ(50px);
				}
				50% {
					transform: translateY(-10px) translateZ(80px);
				}
			}

			.stat-card:hover {
				transform: translateY(-10px) translateZ(100px) scale(1.05);
				border-color: var(--primary);
				box-shadow: var(--shadow-lg);
			}

			.stat-number {
				font-family: var(--font-primary);
				font-size: clamp(1.2rem, 3vw, 1.8rem);
				font-weight: 900;
				background: var(--gradient-neon);
				-webkit-background-clip: text;
				background-clip: text;
				-webkit-text-fill-color: transparent;
				line-height: 1.2;
			}

			.stat-label {
				font-size: 1rem;
				color: var(--gray);
				margin-top: 0.5rem;
				text-transform: uppercase;
				letter-spacing: 0.1rem;
			}

			/* Placeholder pour image hero avec animation */
			.hero-image-placeholder {
				position: absolute;
				bottom: -100px;
				right: 5%;
				width: 200px;
				height: 200px;
				background: rgba(26, 26, 46, 0.6);
				border: 2px dashed rgba(0, 240, 255, 0.3);
				border-radius: 20px;
				display: flex;
				align-items: center;
				justify-content: center;
				backdrop-filter: blur(10px);
				animation: imagePlaceholderPulse 3s ease-in-out infinite;
			}

			@keyframes imagePlaceholderPulse {
				0%,
				100% {
					border-color: rgba(0, 240, 255, 0.3);
					transform: scale(1);
				}
				50% {
					border-color: rgba(0, 240, 255, 0.6);
					transform: scale(1.02);
				}
			}

			.placeholder-content {
				text-align: center;
				color: var(--gray);
			}

			.placeholder-icon {
				font-size: 3rem;
				display: block;
				margin-bottom: 1rem;
			}

			.placeholder-content p {
				font-weight: 600;
				margin-bottom: 0.5rem;
			}

			.placeholder-content small {
				font-size: 0.9rem;
				opacity: 0.7;
			}

			/* ============================================
			   SECTIONS
			   ============================================ */
			.section-title {
				text-align: center;
				font-family: var(--font-primary);
				font-size: clamp(2.5rem, 5vw, 4rem);
				font-weight: 900;
				margin-bottom: var(--spacing-lg);
				position: relative;
			}

			.title-glow {
				display: inline-block;
				background: var(--gradient-neon);
				-webkit-background-clip: text;
				background-clip: text;
				-webkit-text-fill-color: transparent;
				position: relative;
			}

			.title-glow::after {
				content: '';
				position: absolute;
				bottom: -10px;
				left: 50%;
				transform: translateX(-50%);
				width: 100px;
				height: 4px;
				background: var(--gradient-neon);
				border-radius: 2px;
				box-shadow: var(--shadow-neon);
			}

			/* ============================================
			   ÉVÉNEMENTS SECTION
			   ============================================ */
			.events-section {
				padding: var(--spacing-xl) 0;
				position: relative;
			}

			.events-grid {
				display: grid;
				grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
				gap: var(--spacing-md);
				margin-top: var(--spacing-lg);
			}

			/* Cartes 3D avec effet de profondeur */
			.event-card {
				position: relative;
				transform-style: preserve-3d;
				transition: all 0.5s ease;
			}

			.card-3d {
				perspective: 1000px;
			}

			.card-inner {
				background: rgba(26, 26, 46, 0.8);
				border-radius: 20px;
				overflow: hidden;
				border: 1px solid rgba(0, 240, 255, 0.2);
				backdrop-filter: blur(10px);
				transition: all 0.5s ease;
				transform-style: preserve-3d;
			}

			.event-card:hover .card-inner {
				transform: rotateY(5deg) rotateX(5deg) translateZ(30px);
				border-color: var(--primary);
				box-shadow:
					0 30px 60px rgba(0, 240, 255, 0.3),
					0 0 40px rgba(0, 240, 255, 0.2);
			}

			.event-image-placeholder {
				width: 100%;
				height: 250px;
				background: linear-gradient(135deg, rgba(102, 126, 234, 0.2), rgba(118, 75, 162, 0.2));
				display: flex;
				align-items: center;
				justify-content: center;
				font-size: 1.2rem;
				color: var(--gray);
				border-bottom: 1px solid rgba(0, 240, 255, 0.1);
			}

			.event-image {
				width: 100%;
				height: 250px;
				object-fit: cover;
				display: block;
				border-bottom: 1px solid rgba(0, 240, 255, 0.1);
				transition: transform 0.5s ease;
			}

			.event-card:hover .event-image {
				transform: scale(1.05);
			}

			.event-content {
				padding: var(--spacing-md);
			}

			.event-date {
				display: inline-block;
				padding: 0.5rem 1rem;
				background: var(--gradient-neon);
				color: var(--dark);
				border-radius: 20px;
				font-weight: 700;
				font-size: 0.9rem;
				margin-bottom: 1rem;
			}

			.event-content h3 {
				font-family: var(--font-primary);
				font-size: 1.5rem;
				margin-bottom: 1rem;
				color: var(--light);
			}

			.event-content p {
				color: var(--gray);
				margin-bottom: 1.5rem;
			}

			.event-tags {
				display: flex;
				gap: 0.5rem;
				margin-bottom: 1.5rem;
				flex-wrap: wrap;
			}

			.tag {
				padding: 0.3rem 1rem;
				background: rgba(0, 240, 255, 0.1);
				border: 1px solid rgba(0, 240, 255, 0.3);
				border-radius: 15px;
				font-size: 0.85rem;
				color: var(--primary);
			}

			.btn-event {
				width: 100%;
				padding: 1rem;
				background: transparent;
				border: 2px solid var(--primary);
				color: var(--primary);
				font-weight: 700;
				border-radius: 10px;
				cursor: pointer;
				transition: all 0.3s ease;
				font-family: var(--font-primary);
			}

			.btn-event:hover {
				background: var(--primary);
				color: var(--dark);
				transform: translateY(-3px);
				box-shadow: var(--shadow-md);
			}

			/* ============================================
			   FEATURES SECTION
			   ============================================ */
			.features-section {
				padding: var(--spacing-xl) 0;
				background: rgba(26, 26, 46, 0.3);
			}

			.features-grid {
				display: grid;
				grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
				gap: var(--spacing-md);
				margin-top: var(--spacing-lg);
			}

			.feature-card {
				background: rgba(26, 26, 46, 0.8);
				padding: var(--spacing-md);
				border-radius: 20px;
				border: 1px solid rgba(0, 240, 255, 0.2);
				text-align: center;
				transition: all 0.5s ease;
				transform-style: preserve-3d;
			}

			.feature-card.floating {
				animation: featureFloat 6s ease-in-out infinite;
			}

			.feature-card:nth-child(1) {
				animation-delay: 0s;
			}
			.feature-card:nth-child(2) {
				animation-delay: 1s;
			}
			.feature-card:nth-child(3) {
				animation-delay: 2s;
			}
			.feature-card:nth-child(4) {
				animation-delay: 3s;
			}
			.feature-card:nth-child(5) {
				animation-delay: 4s;
			}
			.feature-card:nth-child(6) {
				animation-delay: 5s;
			}

			@keyframes featureFloat {
				0%,
				100% {
					transform: translateY(0) rotateZ(0deg);
				}
				50% {
					transform: translateY(-20px) rotateZ(2deg);
				}
			}

			.feature-card:hover {
				transform: translateY(-15px) scale(1.05) rotateZ(0deg);
				border-color: var(--primary);
				box-shadow: var(--shadow-lg);
			}

			.feature-icon {
				font-size: 4rem;
				margin-bottom: 1rem;
				display: inline-block;
				animation: iconSpin 10s linear infinite;
			}

			.feature-card:hover .feature-icon {
				animation: iconBounce 0.5s ease-in-out;
			}

			@keyframes iconSpin {
				0%,
				90%,
				100% {
					transform: rotate(0deg);
				}
				95% {
					transform: rotate(360deg);
				}
			}

			@keyframes iconBounce {
				0%,
				100% {
					transform: scale(1);
				}
				50% {
					transform: scale(1.2);
				}
			}

			.feature-card h3 {
				font-family: var(--font-primary);
				font-size: 1.3rem;
				margin-bottom: 1rem;
				color: var(--primary);
			}

			.feature-card p {
				color: var(--gray);
				line-height: 1.6;
		}

		/* ============================================
		   GAMES PROMO SECTION
		   ============================================ */
		.games-promo-section {
			padding: var(--spacing-xl) 0;
			position: relative;
		}

		.games-promo-card {
			background: var(--gradient-gaming);
			padding: var(--spacing-lg) var(--spacing-md);
			border-radius: 30px;
			text-align: center;
			box-shadow: var(--shadow-lg);
			position: relative;
			overflow: hidden;
			animation: cardFloat 5s ease-in-out infinite;
		}

		.games-promo-title {
			font-family: var(--font-primary);
			font-size: clamp(2rem, 5vw, 3.5rem);
			font-weight: 900;
			color: var(--dark);
			margin-bottom: 1.5rem;
			display: flex;
			align-items: center;
			justify-content: center;
			gap: 1rem;
		}

		.games-icon {
			font-size: 3rem;
			animation: float 3s ease-in-out infinite;
		}

		.games-promo-text {
			font-size: 1.3rem;
			color: var(--dark);
			margin-bottom: 2rem;
			opacity: 0.95;
			line-height: 1.8;
		}

		.games-promo-text strong {
			color: var(--dark);
			font-weight: 900;
		}

		.btn-games-promo {
			position: relative;
			display: inline-block;
			padding: 1.2rem 3rem;
			font-size: 1.3rem;
			font-weight: 700;
			font-family: var(--font-primary);
			background: var(--dark);
			color: var(--primary);
			border: none;
			border-radius: 50px;
			cursor: pointer;
			text-decoration: none;
			transition: all 0.3s ease;
			overflow: hidden;
			transform-style: preserve-3d;
			box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
		}

		.btn-games-promo span {
			position: relative;
			z-index: 2;
		}

		.btn-games-promo:hover {
			transform: translateY(-5px) scale(1.05);
			box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5);
		}

		.btn-games-promo .btn-glow {
			position: absolute;
			top: 0;
			left: -100%;
			width: 100%;
			height: 100%;
			background: linear-gradient(
				90deg,
				transparent,
				rgba(0, 240, 255, 0.3),
				transparent
			);
			transition: left 0.5s ease;
		}

		.btn-games-promo:hover .btn-glow {
			left: 100%;
		}

		/* ============================================
		   COMMUNITY SECTION
		   ============================================ */
			.community-section {
				padding: var(--spacing-xl) 0;
			}

			.community-grid {
				display: grid;
				grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
				gap: var(--spacing-md);
				margin-top: var(--spacing-lg);
			}

			.community-card {
				position: relative;
				height: 350px;
				border-radius: 20px;
				overflow: hidden;
				cursor: pointer;
				transform-style: preserve-3d;
				transition: all 0.5s ease;
			}

			.pulse-3d {
				animation: pulse3D 4s ease-in-out infinite;
			}

			.community-card:nth-child(1) {
				animation-delay: 0s;
			}
			.community-card:nth-child(2) {
				animation-delay: 1s;
			}
			.community-card:nth-child(3) {
				animation-delay: 2s;
			}
			.community-card:nth-child(4) {
				animation-delay: 3s;
			}

			@keyframes pulse3D {
				0%,
				100% {
					transform: scale(1) translateZ(0);
				}
				50% {
					transform: scale(1.05) translateZ(20px);
				}
			}

			.community-image-placeholder {
				width: 100%;
				height: 100%;
				background: linear-gradient(135deg, rgba(79, 172, 254, 0.3), rgba(0, 242, 254, 0.3));
				display: flex;
				align-items: center;
				justify-content: center;
				font-size: 1.2rem;
				color: var(--gray);
				transition: transform 0.5s ease;
			}

			.community-overlay {
				position: absolute;
				bottom: 0;
				left: 0;
				right: 0;
				padding: var(--spacing-md);
				background: linear-gradient(to top, rgba(10, 10, 15, 0.95), transparent);
				transform: translateY(100%);
				transition: transform 0.5s ease;
			}

			.community-card:hover .community-overlay {
				transform: translateY(0);
			}

			.community-card:hover .community-image-placeholder {
				transform: scale(1.1);
			}

			.community-overlay h3 {
				font-family: var(--font-primary);
				font-size: 1.3rem;
				color: var(--primary);
				margin-bottom: 0.5rem;
			}

			.community-overlay p {
				color: var(--gray);
			}

			/* ============================================
			   GALLERY SECTION
			   ============================================ */
			.gallery-section {
				padding: var(--spacing-xl) 0;
				background: rgba(26, 26, 46, 0.3);
			}

			.gallery-grid {
				display: grid;
				grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
				gap: var(--spacing-sm);
				margin-top: var(--spacing-lg);
			}

			.gallery-item {
				position: relative;
				height: 300px;
				border-radius: 15px;
				overflow: hidden;
				cursor: pointer;
				transition: all 0.5s ease;
				transform-style: preserve-3d;
			}

			.gallery-item:hover {
				transform: scale(1.05) translateZ(30px);
				box-shadow: var(--shadow-lg);
			}

			.gallery-placeholder {
				width: 100%;
				height: 100%;
				background: linear-gradient(135deg, rgba(102, 126, 234, 0.3), rgba(118, 75, 162, 0.3));
				display: flex;
				align-items: center;
				justify-content: center;
				font-size: 1.2rem;
				color: var(--gray);
				transition: all 0.5s ease;
			}

			.gallery-item:hover .gallery-placeholder {
				transform: scale(1.1);
			}

			/* ============================================
			   NEWSLETTER SECTION
			   ============================================ */
			.newsletter-section {
				padding: var(--spacing-xl) 0;
			}

			.newsletter-card {
				background: var(--gradient-gaming);
				padding: var(--spacing-lg) var(--spacing-md);
				border-radius: 30px;
				text-align: center;
				box-shadow: var(--shadow-lg);
				transform-style: preserve-3d;
				animation: newsletterFloat 5s ease-in-out infinite;
			}

			@keyframes newsletterFloat {
				0%,
				100% {
					transform: translateY(0) rotateX(0deg);
				}
				50% {
					transform: translateY(-10px) rotateX(2deg);
				}
			}

			.newsletter-card h2 {
				font-family: var(--font-primary);
				font-size: 2.5rem;
				margin-bottom: 1rem;
				color: var(--dark);
			}

			.newsletter-card p {
				color: var(--dark);
				font-size: 1.1rem;
				margin-bottom: 2rem;
				opacity: 0.9;
			}

			.event-info {
				max-width: 600px;
				margin: 0 auto;
				text-align: left;
			}

			.event-info p {
				margin-bottom: 1rem;
				font-size: 1.2rem;
				color: var(--dark);
			}

			.newsletter-form {
				display: flex;
				gap: 1rem;
				max-width: 600px;
				margin: 0 auto;
				flex-wrap: wrap;
				justify-content: center;
			}

			.newsletter-input {
				flex: 1;
				min-width: 250px;
				padding: 1rem 1.5rem;
				border: none;
				border-radius: 50px;
				font-size: 1rem;
				background: rgba(255, 255, 255, 0.9);
				color: var(--dark);
			}

			.newsletter-input:focus {
				outline: none;
				box-shadow: 0 0 20px rgba(255, 255, 255, 0.5);
			}

			/* ============================================
			   FOOTER
			   ============================================ */
			.footer {
				background: var(--dark-2);
				padding: var(--spacing-lg) 0 var(--spacing-md);
				border-top: 1px solid rgba(0, 240, 255, 0.1);
			}

			.footer-grid {
				display: grid;
				grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
				gap: var(--spacing-md);
				margin-bottom: var(--spacing-md);
			}

			.footer-col h3,
			.footer-col h4 {
				font-family: var(--font-primary);
				color: var(--primary);
				margin-bottom: 1rem;
			}

			.footer-col p {
				color: var(--gray);
				line-height: 1.6;
			}

			.social-links {
				display: flex;
				gap: 1rem;
				margin-top: 1rem;
			}

			.social-icon {
				width: 50px;
				height: 50px;
				display: flex;
				align-items: center;
				justify-content: center;
				background: rgba(0, 240, 255, 0.1);
				border: 1px solid rgba(0, 240, 255, 0.3);
				border-radius: 10px;
				font-size: 1.5rem;
				transition: all 0.3s ease;
				text-decoration: none;
			}

			.social-icon:hover {
				background: var(--primary);
				transform: translateY(-5px) rotate(360deg);
				box-shadow: var(--shadow-md);
			}

			.footer-col ul {
				list-style: none;
			}

			.footer-col ul li {
				margin-bottom: 0.5rem;
			}

			.footer-col ul li a {
				color: var(--gray);
				text-decoration: none;
				transition: color 0.3s ease;
			}

			.footer-col ul li a:hover {
				color: var(--primary);
			}

			.footer-bottom {
				text-align: center;
				padding-top: var(--spacing-md);
				border-top: 1px solid rgba(0, 240, 255, 0.1);
				color: var(--gray);
			}

			/* ============================================
			   SCROLL TO TOP
			   ============================================ */
			.scroll-to-top {
				position: fixed;
				bottom: 30px;
				right: 30px;
				width: 50px;
				height: 50px;
				background: var(--gradient-neon);
				color: var(--dark);
				border: none;
				border-radius: 50%;
				font-size: 1.5rem;
				font-weight: 900;
				cursor: pointer;
				opacity: 0;
				transform: translateY(100px);
				transition: all 0.3s ease;
				box-shadow: var(--shadow-md);
				z-index: 999;
			}

			.scroll-to-top.visible {
				opacity: 1;
				transform: translateY(0);
			}

			.scroll-to-top:hover {
				transform: translateY(-5px) scale(1.1);
				box-shadow: var(--shadow-lg);
			}

			/* ============================================
			   RESPONSIVE
			   ============================================ */
			@media (max-width: 768px) {
				.nav-links {
					display: none;
				}

				.mobile-menu-btn {
					display: flex;
				}

				.hero-title {
					font-size: 3rem;
				}

				.hero-image-placeholder {
					display: none;
				}

				.events-grid,
				.features-grid,
				.community-grid,
				.gallery-grid {
					grid-template-columns: 1fr;
				}

				.hero-stats {
					flex-direction: column;
					align-items: center;
				}

				.newsletter-form {
					flex-direction: column;
				}

				.newsletter-input {
					width: 100%;
				}
			}

			@media (max-width: 480px) {
				.container {
					padding: 0 var(--spacing-sm);
				}

				.hero-title {
					font-size: 2rem;
				}

				.hero-subtitle {
					font-size: 1rem;
					letter-spacing: 0.1rem;
				}

				.section-title {
					font-size: 2rem;
				}

				.btn {
					padding: 0.8rem 1.5rem;
					font-size: 0.9rem;
				}
			}
		</style>

		<script>
			// Scroll to top functionality
			const scrollToTopBtn = document.getElementById('scrollToTop');

			window.addEventListener('scroll', () => {
				if (window.pageYOffset > 300) {
					scrollToTopBtn?.classList.add('visible');
				} else {
					scrollToTopBtn?.classList.remove('visible');
				}
			});

			scrollToTopBtn?.addEventListener('click', () => {
				window.scrollTo({
					top: 0,
					behavior: 'smooth',
				});
			});

			// Smooth scroll pour les liens d'ancrage
			document.querySelectorAll<HTMLAnchorElement>('a[href^="#"]').forEach((anchor) => {
				anchor.addEventListener('click', function (e: Event) {
					e.preventDefault();
					const href = (e.currentTarget as HTMLAnchorElement).getAttribute('href');
					const target = href ? document.querySelector(href) : null;
					if (target) {
						target.scrollIntoView({
							behavior: 'smooth',
							block: 'start',
						});
					}
				});
			});

			// Animation au scroll
			const observerOptions = {
				threshold: 0.1,
				rootMargin: '0px 0px -50px 0px',
			};

			const observer = new IntersectionObserver((entries) => {
				entries.forEach((entry) => {
					if (entry.isIntersecting) {
						(entry.target as HTMLElement).style.opacity = '1';
						(entry.target as HTMLElement).style.transform = 'translateY(0)';
					}
				});
			}, observerOptions);

			// Observer les éléments pour l'animation au scroll
			document
				.querySelectorAll<HTMLElement>('.event-card, .feature-card, .community-card, .gallery-item')
				.forEach((el) => {
					el.style.opacity = '0';
					el.style.transform = 'translateY(30px)';
					el.style.transition = 'opacity 0.6s ease, transform 0.6s ease';
					observer.observe(el);
				});
		</script>
	</body>
</html>
